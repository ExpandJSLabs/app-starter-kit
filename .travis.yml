language: node_js
sudo: false
env:
    - CXX=g++-4.8
addons:
    firefox: latest
    apt:
        sources:
            - google-chrome
            - ubuntu-toolchain-r-test
    packages:
        - google-chrome-stable
        - g++-4.8
node_js:
    - "4.2"
    - "5.1"
before_script:
    - export DISPLAY=:99.0
    - sh -e /etc/init.d/xvfb start
    - sleep 3
    - npm install bower -g
    - npm install gulp -g
    - bower install
script: gulp
